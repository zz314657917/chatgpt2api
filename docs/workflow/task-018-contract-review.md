# Task 018 Contract Review

### PASS: task-018-semantic-prompt-split

- 结构化 schema、数量优先级、群组语义和多维度选择规则明确。
- 后端响应只做向后可忽略的可选字段扩展，请求、计费与任务编排不变。
- 验收同时覆盖 parser/service、HTTP、Canvas 展示和 Task-017 回归，可进入 build。
