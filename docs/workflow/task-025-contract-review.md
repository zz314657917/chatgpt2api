### PASS: task-025-bead-project-cloud-storage

- Contract 将首个 Sprint 限定为个人私有工程持久化、REST API、RBAC/分析映射和前端 API 客户端，不混入上游 UI 迁移、素材库交互或移动端实现。
- 公共接口、owner 隔离、revision 409、摘要/完整文档边界、30 项/156 边长/20 图层/5 MiB 约束和素材引用白名单均有明确可测试标准。
- 存储布局固定为每用户独立索引和独立工程文档，并要求索引写入失败时恢复内存与落盘状态，覆盖计划中特别要求的失败回滚。
- RBAC 明确区分默认普通用户新增权限与现有自定义角色不自动扩权，避免权限升级范围不清。
- 验收命令覆盖 service、HTTP、权限/分析、前端类型构建和差异检查；页面与浏览器验收留给后续 Sprint。
