### PASS: task-026-bead-workbench-port

- Contract 固定上游 commit、许可证、允许迁入文件类型和禁止迁入的脚本/文档/截图，第三方来源边界明确。
- 工作台能力对齐用户计划，同时把素材库互通、1200ms 自动保存/409 冲突和完整移动抽屉留给 Task-027，避免本 Sprint 混入两个独立集成风险面。
- 路由、导航、中文主题、CSS namespace、React 模块导入、Three.js named imports/独立 chunk、导出器动态加载和 bundle 预算都有可验证标准。
- 只允许新增 `three`；PNG/PDF/XLSX/JSON 复用上游纯 TS 导出能力，不扩大依赖面。
- 浏览器场景覆盖项目列表、转换编辑、图层、3D 非空像素和全部导出；移动三视口完整验收保留给 Task-027/028。
